[project]
name = "calendar-puzzle"
version = "0.1.0"
description = "Whole year puzzle solver"
requires-python = ">=3.11"
dependencies = [
    "matplotlib>=3.10.8",
]

[project.scripts]
solve = "calendar_puzzle_solver:main"
